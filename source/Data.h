#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <string>

using std::string;
using std::cout;
using std::endl;
using std::cerr;

class Data{
    // _data();
    // _nb_features();
    // _nb_samples();
public:
    // add();
    // aspire_les_donnees();
    // Data();
    void load(const string& file_name);
    // nbSamples();
    // operator();
    // scale();
    // toString();
};